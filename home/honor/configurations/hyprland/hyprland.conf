# Hyprland config entrypoint (kept tiny on purpose).
#
# - `hyprland.base.conf`: mostly static defaults / template-ish settings
# - `hyprland.custom.conf`: your actual tweaks (programs, env, input, binds, rules)
#
# Managed by Home Manager in this repo.

source = ~/.config/hypr/hyprland.base.conf
source = ~/.config/hypr/hyprland.custom.conf